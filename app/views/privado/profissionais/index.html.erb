<div id="crud_index">

	<div id="lista">
		
		<div id="lista_header" class="section_header">
			<%= link_to(image_tag('botoes/add.png'),new_profissional_path, remote: true, style: 'float: right') %>
			<h2 class="titulo"><%= Profissional.model_names[:human_plural] %></h2>
			<%= filtra_html('#indice .item', watermark: t('crud.pesquisa')) %>
		</div>
		<ul id="indice">
			<% @profissionais.each do |s| %>
				<li id='<%= s.id %>' class='item pode_selecionar'>
					<%= s.nome %>
				</li>
			<% end %>
		</ul>
	</div>
	
	<div id="direita" class="resumos">
		<%= render(partial: 'resumo', collection: @profissionais, as: :profissional) %>
		<div id="exibe_form" class="resumo"></div>
	</div>
</div>

<%= javascript_tag("Crud.exibeResumo({id: #{params[:selecionar] || @profissionais.first.id}});") if @profissionais.any? %>

